<div class="container">
  <div class="row">
    <div class="span12">
      <% provide(:title, 'Reviews') %>

      <% @reviews.each do |review| %>

          <%= render 'shared/preview_review', object: review %>

      <% end %>

      <%= will_paginate %>
    </div>
  </div>
</div>

