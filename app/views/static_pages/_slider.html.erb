<div id="myCarousel" class="carousel slide">

  <!-- Carousel items -->
  <div class="carousel-inner">

    <!-- First article -->
    <div class="active item"><%= image_tag 'slider3.jpg', width: 1000, height: 100 %>
      <div class="carousel-caption">
        <h4>Apple's Iphone exposed!</h4>

        <p><%= @articles[0].content %></p>
      </div>
    </div>

    <!-- Second article -->
    <div class="item"><%= image_tag 'slider2.jpg', width: 1000, height: 100 %>
      <div class="carousel-caption">
        <h4>Apple's Iphone exposed!</h4>

        <p>Random Apple crap goes here, without much further adue.</p>
      </div>
    </div>

    <!-- Third article -->
    <div class="item"><%= image_tag 'slider1.jpg', width: 1000, height: 100 %>
      <div class="carousel-caption">
        <h4>Apple's Iphone exposed!</h4>

        <p>Random Apple crap goes here, without much further adue.</p>
      </div>
    </div>

  </div>
  <!-- Carousel nav -->
  <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
  <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
</div>